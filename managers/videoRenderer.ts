import { renderMedia } from '@remotion/renderer';
import { bundle } from '@remotion/bundler';
import path from 'node:path';

const entry = path.join(process.cwd(), './video-prod', './index.ts')

export default async function renderVideo() { 
        
}